
    <div class="d-flex justify-content-center">
    <h1 class="h3 mb-3 fw-normal mt-5 pt-5 text-body-emphasis fw-bold display-6 text-center col-4">Inicia sesión para navegar en nuestra tienda</h1>
    </div>
    <main class="form-signin m-auto col-4 mb-5 pb-5">
        <form class="mt-5" action="/api/login" method="post">
            <h1 class="h3 mb-3 fw-normal mt-5 pt-5 text-body-emphasis">Login</h1>
            <div class="form-floating mt-4">
                <input type="email" class="form-control" name="email" id="email" placeholder="name@example.com" required>
                <label for="email">Email</label>
            </div>
            <div class="form-floating mb-5 mt-3">
                <input type="password" class="form-control" name="pass" id="pass" placeholder="Contraseña" required>
                <label for="pass">Password</label>
            </div>

            <div class="form-check text-start my-3">
                <input class="form-check-input" type="checkbox" value="remember-me" id="flexCheckDefault">
                <label class="form-check-label" for="flexCheckDefault">
                    Remember me
                </label>
            </div>
            <button class="btn btn-primary w-100 py-2" type="submit">Sign in</button>
        </form>
        <button class="mt-3 btn btn-dark text-decoration-none w-100 py-2"><a class="text-white" href="/api/github">Github</a></button>
            {{#if message}}
            <p class="text-center mt-3 text-danger">{{message}}.</p>
            {{/if}}
            <p class="text-center mt-4">¿Olvidaste tu contraseña? <a href="/reset-password">Restrablecer Contraseña</a>
            </p>
            <p class="text-center mt-4 mb-5"><a href="/register">¿No tienes una cuenta? Registrate</a></p>
    </main>
