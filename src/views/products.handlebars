
  <div class="col-5 mb-3 mt-5 p-3 ms-4">
    <h1 class="display-4 fw-bold lh-1 text-body-emphasis">Encuentra lo que</h1>
    <h1 class="display-4 fw-bold lh-1  text-warning">buscas</h1>
  </div>

  <div class=" rounded-end-circle div__dividier col-6 "></div>
  <div class="d-flex flex-wrap col-12 justify-content-evenly mt-5">
    {{#each products}}
    <div class="card col-3 m-3 d-flex div__container--card flex-column rounded-3">
      <div class="d-flex justify-content-evenly flex-column">
        <div class="w-100 ">
          <img src="{{this.img}}" class="img__simulator img-fluid rounded-3" alt="{{this.title}}">
        </div>
        <div class="card-body d-flex flex-column align-content-end">
          <h5 class="card-title">{{this.title}}</h5>
          <p class="card-text card__container--text">{{this.description}}</p>
          <p class="fs-5 fw-semibold">$ {{this.price}}</p>
        </div>
      </div>
      <button class="rounded-2 btn btn-secondary btn__addToCart" data-product-id="{{this._id}}"
        data-user-cart="{{../user.cart}}">Agregar al carrito</button>
    </div>

    {{/each}}
  </div>

  <div class="d-flex col-12 justify-content-center mt-3 mb-3">

    {{#if hasPrevPage}}
    <a class="col-2 text-center" href="{{prevLink}}">Anterior</a>
    {{/if}}
    <p class="col-2 text-center">Pagina {{currentPage}} de {{totalPages}}</p>
    {{#if hasNextPage}}
    <a class="col-2 text-center" href="{{nextLink}}">Siguiente</a>
    {{/if}}

  </div>
  <script src="js/mainProducts.js"></script>
